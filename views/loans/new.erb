<!-- <h1>Check a book out</h1>

<form action="/loans" method="post">
  <label>
  Select a book:
    <select name="book_id">
      <% for book in @books %>
      <option value="<%= book.id %>"><%= book.title %></option>
      <% end %>
    </select>
  </label>

  <label>
    Select a member:
    <select name="member_id">
      <% for member in @members %>
      <option value="<%= member.id %>"><%= member.last_name %></option>
      <% end %>
    </select>
  </label>

  <input class="btn btn--action" type='submit' value="loan made!">
</form> -->

<form method="post" action="/loans">
  <label for="first-name-field">First Name:</label>
  <input
  type="text"
  name="first_name"
  id="first-name-field"
  />

  <label for="last-name-field">Last Name:</label>
  <input
  type="text"
  name="last_name"
  id="last-name-field"
  />

  <label for="postcode-field">Post Code:</label>
  <input
  type="text"
  name="post_code"
  id="postcode-field"
  />

  <label for="email-address-field">Email Address:</label>
  <input
  type="text"
  name="last_name"
  id="email-address-field"
  />

  <label for="active-membership-field">Active Membership:</label>
  <input
  type="boolean"
  name="active_membership"
  id="active-membership-field"
  />

  <input type="submit" value="Loan book out!">
</form>
